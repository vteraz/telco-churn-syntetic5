name: Telco Churn ML Pipeline Vasyl Teraz Module 6

on:
    push:
        branches:
            - develop
            - 'module-**'

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - name: One
              run: uname -a
            - name: Two
              run: ps aux
